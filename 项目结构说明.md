# Test-Entry 项目结构说明

## 概述

测试入口向导工具，通过 5 步操作快速生成测试 URL 并跳转。

- **技术栈**：Vue 3 + Vite + TailwindCSS
- **无路由库**，步骤切换通过 `currentStep` ref + `v-if` 实现
- **无状态管理库**，状态在 `useWizard.js` 模块顶层定义，所有组件共享同一组引用

## 文件结构

```
src/
├── main.js                          # 入口，挂载 App
├── App.vue                          # 主框架：header + StepIndicator + 5 个 Step 组件
├── style.css                        # Tailwind 入口
├── composables/
│   └── useWizard.js                 # 全局状态与逻辑（模块级 ref，核心文件）
├── config/
│   ├── environments.js              # 环境配置：vs2 / xs / prod
│   ├── languages.js                 # 语言配置：ch / en / jpn / fra / rus / spa / ind / vie
│   └── projects/
│       ├── index.js                 # 项目注册入口，导出 projects 数组
│       ├── h5.js                    # H5 商城配置
│       ├── admin.js                 # Admin 后台配置
│       ├── shop.js                  # Shop PC 配置
│       └── finance.js               # Finance 财务中心配置
├── components/
│   ├── StepIndicator.vue            # 步骤条（1-5），可点击回退
│   ├── StepToken.vue                # Step1：输入 dcpToken
│   ├── StepProject.vue              # Step2：选择项目
│   ├── StepEnv.vue                  # Step3：选择环境
│   ├── StepRoute.vue                # Step4：选择路由
│   ├── StepConfirm.vue              # Step5：确认并跳转（含语言选择和自定义参数）
│   └── PhonePreview.vue             # H5 手机模拟器弹窗（iframe 加载）
└── utils/
    └── colors.js                    # 颜色工具，gc(key) 返回 Tailwind class 对象
```

## 向导流程

| 步骤 | 组件 | 说明 |
|------|------|------|
| 1 | StepToken | 输入 dcpToken，localStorage 缓存，回车或点击确认进入下一步 |
| 2 | StepProject | 2 列网格展示项目卡片，点击选择后自动进入下一步 |
| 3 | StepEnv | 列表展示环境，点击选择后自动进入下一步 |
| 4 | StepRoute | 按 routeGroups 分组展示路由，点击路由自动进入下一步 |
| 5 | StepConfirm | 跳转参数区（语言选择 + 自定义参数），汇总展示选择信息 + 最终 URL，可返回修改或确认跳转 |

**H5 项目特殊行为**：点击「确认跳转」后，不打开新标签，而是弹出手机模拟器窗口（iframe 加载页面）。支持切换 iPhone 尺寸：

| 设备 | CSS 视口 | Dynamic Island |
|------|----------|----------------|
| iPhone SE | 375 x 667 | 无 |
| iPhone 15 Pro | 393 x 852 | 有 |
| iPhone 16 Pro Max | 430 x 932 | 有 |

设备配置在 `useWizard.js` 的 `devices` 数组中，新增机型只需加一条记录。

步骤条可点击回退到任意已完成步骤（只能回退不能前进）。

## 核心数据流

`useWizard.js` 在模块顶层定义所有 ref：

```
currentStep        — 当前步骤（1-5）
dcpToken           — 用户输入的 token
selectedEnvId      — 选中的环境 ID
selectedProjectId  — 选中的项目 ID
selectedRoutePath  — 选中的路由 path
```

每次调用 `useWizard()` 返回同一组引用（模块级单例）。

**URL 拼接规则**（`finalUrl` computed）：

```
${baseUrl}#${routePath}?language=ch&dcpToken=xxx&key1=value1
```

- `language` 语言参数，和 dcpToken、自定义参数一起拼在 `#` 后面，默认 `ch`，在确认页可切换
- `dcpToken` 固定拼接（Step1 输入）
- `customParams` 自定义参数（StepConfirm 页面添加，支持任意 key=value，相同 key 覆盖）

## 数据结构

### 环境配置（environments.js）

```js
{ id: 'vs2', name: 'VS2', tag: 'PHP', color: 'blue' }
```

- `color` 对应 `colors.js` 中的 key，用于 StepEnv 的样式

### 项目配置（projects/*.js）

```js
{
  id: 'h5',
  name: 'H5 商城',
  desc: 'marketing-web-h5',
  urls: {
    vs2: 'https://wxtest.51tyty.com/marketing/front/h5/index.html',
    xs: 'https://wxxs.51tyty.com/marketing/front/h5/index.html',
    prod: 'https://wechat.51tyty.com/marketing/front/h5/index.html',
  },
  routeGroups: [
    { module: '商城模块', routes: [
      { label: '商城首页', path: '/market' },
      { label: '商品详情', path: '/goodsDetails', tip: '需传入商品ID' },
      // ...
    ]},
  ],
}
```

## 常见修改指南

| 需求 | 修改文件 |
|------|----------|
| 新增项目 | `src/config/projects/` 新建 `.js` 文件，在 `index.js` 中 import 并加入数组 |
| 新增环境 | `environments.js` 添加环境；每个项目的 `urls` 补对应 URL；`colors.js` 加颜色映射 |
| 新增/修改语言 | `languages.js` 中添加语言记录 |
| 新增/修改路由 | 对应项目的 `routeGroups` 中添加或修改 |
| 修改 URL 拼接逻辑 | `useWizard.js` 中的 `finalUrl` computed |
| 修改步骤流程 | `App.vue`（v-if 步骤切换）+ `StepIndicator.vue`（步骤条） |
| 修改某步骤 UI | 对应的 `Step*.vue` 组件 |
